#ifndef UI_SCALE_H
#define UI_SCALE_H

/* Global UI scale factor: physical pixels per logical pixel (2.0 on a
 * 1920x1080 screen, 1.0 on the smaller modes the loader falls back to). */
extern float ui_scale;

#endif
